# Quick Start

1. Deploy the project files directly to the domain server root.
2. Visit `/install.php`.
3. Submit the database credentials from your hosting provider.
4. Visit `/index.php` or `/`.
5. Visit `/admin/` and change the default admin password.

Runtime database credentials must live only in generated `config.php`.
